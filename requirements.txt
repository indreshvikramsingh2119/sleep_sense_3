# Sleep Sense Dashboard - PyQt5 Requirements

PyQt5>=5.15.10
PyQtWebEngine>=5.15.6
pyqtgraph>=0.13.3
numpy>=1.26.0
pandas>=1.5.0
reportlab>=4.0.0
Pillow>=10.0.0
scipy>=1.11.0
scikit-learn>=1.4.0
matplotlib>=3.8.0
joblib>=1.3.0

# Note: SQLite Viewer is a VS Code extension (qwtel.sqlite-viewer), not a Python package.
# The app itself uses Python's built-in sqlite3 module, so no extra pip dependency is needed.
